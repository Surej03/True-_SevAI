npm install -D tailwindcss postcss autoprefixer - Install tailwindcss and its peer dependencies, then generate your 
tailwind.config.js and postcss.config.js files.

npx tailwindcss init -p 

for animation - framer motion 
npm install motion
import {motion} from "framer-motion"
wherever you want to add motion it should be before the opening and closing tag
eg. <motion.div></motion.div>